# Output Explanation
```json
{
    "Attribution": "A matrix of shape len(ground_truth_tokens)*len(prompt_tokens) representing the input's influence on the output", 
    "prompt_tokens": "Input tokens", 
    "ground_truth_tokens": "Output tokens"
}
```