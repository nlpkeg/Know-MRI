# Output Explanation
```json
{
    "origin_data": {
        "Contribution_of_the_neuron": "An np.array with shape: (num_layers × neurons_per_layer) representing contribution scores of each neuron",
        "Neuron_index": "A list of tuples where each tuple contains: (layer_index, neuron_position_in_layer)"
    }
}
```