# Output Explanation
```json
{
    "origin_data": {
        "sample_tokens": "Tokens obtained from splitting the sample",
        "topk_embeddings": "A 3D list where each 2D sublist has dimensions [topk, 2], representing the top k dimensions and their corresponding activation values for each token (each entry being a length-2 list of [dimension, activation])"
    }
}
```