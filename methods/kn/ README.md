# Output Explanation
```json
{
    "origin_data": {
        "Contribution_of_the_neuron": "An np.array of shape (num_layers × neurons_per_layer) representing the contribution score of each neuron",
        "Neuron_index": "A list of tuples where each tuple contains: (layer_index, neuron_position) indicating the layer number and position of identified neurons"
    }
}
```